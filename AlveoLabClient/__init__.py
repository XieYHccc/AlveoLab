"""AlveoLab desktop client (PySide6 + PyVista).

The client is a thin UI shell on top of the `AlveoLab` algorithm package.
Dependency direction is strict: `AlveoLabClient.*` may import from `AlveoLab.*`,
but never the reverse.
"""
